#include "meshoptim/data.h"
namespace meshoptim
{
  // mesh::mesh(std::size_t element_count) : 
  //   vertices(element_count) {
  //     std::size_t data_length = element_count * 3 * string_len;
  //     this->mesh_data = std::string(data_length, '0');
  //   }
}